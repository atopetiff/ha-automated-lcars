alias: SetRgb-Red
sequence:
  - action: script.setrgb
    data:
      red: 0
      blue: 0
      green: 255
      entities: "{{ entities }}"
description: ""
fields:
  entities:
    selector:
      entity:
        multiple: true
    name: entities
    required: true
icon: mdi:led-strip
